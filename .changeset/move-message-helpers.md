---
"@realtalk-ai/core": minor
"@realtalk-ai/embed": patch
---

Move `hasPendingSubTasks` from the embed package into core so any SDK consumer can tell whether the agent is still working on sub-tasks.
