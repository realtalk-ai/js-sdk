---
"@realtalk-ai/embed": patch
---

Declare the bundled `@realtalk-ai/core` and `@realtalk-ai/react` packages as exact dependencies so every release of either automatically triggers an embed rebuild and release.
